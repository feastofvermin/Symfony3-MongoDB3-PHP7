doctrine_mongodb:
    connections:
        default:
            server: "%mongodb_server%"
            options: {}
    default_database: admin
    document_managers:
        default:
            auto_mapping: true